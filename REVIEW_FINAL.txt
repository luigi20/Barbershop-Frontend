REVISÃO FINAL DO GIT DIFF

Escopo revisado: working tree atual e delta main...HEAD.

CRÍTICO

Nenhum finding crítico.

IMPORTANTE

1. Clientes não trata 401 e 403 explicitamente

src/hooks/use-customers.ts converte qualquer falha em status "error" e descarta o
statusCode fornecido por CustomersClientError.

Consequências:

- 401 não redireciona diretamente para login pelo fluxo de Clientes; atualmente
  isso depende indiretamente da requisição paralela de /api/auth/me feita pelo
  DashboardShell.
- 403 não possui estado específico de acesso negado.
- Loading, empty e erro genérico estão corretamente implementados.

2. Documentação possui classificações inconsistentes

- docs/ARCHITECTURE.md ainda classifica Clientes como PLANEJADO, embora a
  integração já esteja implementada e esteja CONFIRMADO em CURRENT_STATE.md e
  API.md.
- docs/CURRENT_STATE.md classifica Agenda, Serviços e Financeiro como PLANEJADO,
  contrariando a classificação solicitada de BLOQUEADO.
- MFA e Profile aparecem corretamente como BLOQUEADO nas seções principais, mas
  reaparecem como PLANEJADO no roadmap de CURRENT_STATE.md, deixando o estado
  ambíguo.
- Equipe/Memberships e Plan estão corretamente PLANEJADOS.

MELHORIA

1. Tipos exportados aparentemente sem consumidores

SelectEntityClientRequest e SelectEntityBackendRequest, em src/types/auth.ts,
não possuem usos encontrados. Não causam regressão, mas são candidatos a código
morto.

CONFIRMAÇÕES

- login_token não chega mais a Client Components ou props.
- A resposta pública do signin contém apenas requires_entity_selection e
  entities.
- challenge_token é HttpOnly, restrito a /api/auth, e lido server-side por
  select-entity.
- O browser envia apenas entity_id na seleção da entidade.
- /clientes chama somente /api/customers.
- Nenhum entity_id é enviado pelo browser no fluxo de Clientes.
- O BFF de Clientes usa withAuthRoute.
- A resposta pública é reconstruída explicitamente com somente os oito campos do
  contrato.
- Signin com uma entidade e com múltiplas entidades está coerente por inspeção
  estática.
- Refresh realiza uma tentativa e um único retry.
- Logout faz revogação remota best-effort e limpeza local incondicional.
- Proxy aceita presença de access ou refresh e não considera challenge como
  sessão autenticada.
- Nenhum any, token em log, localStorage, sessionStorage, chamada direta ao
  NestJS em Client Components ou mock de clientes usado por /clientes.
- Nenhum import morto foi reportado pelo lint.

VALIDAÇÕES

- npm run lint: passou, com 3 warnings não bloqueantes.
- npm run lint:prettier:check: passou.
- npm run build: passou.
- git diff --check e git diff main...HEAD --check: passaram.
- Nenhuma correção foi implementada, e nenhum commit ou push foi realizado.
